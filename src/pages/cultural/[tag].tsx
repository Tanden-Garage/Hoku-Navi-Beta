import CulturalTagPage from "@/components/page/Cultural/Tag"

import { Layout } from "@/components/layout"

CulturalTagPage.getLayout = (page) => <Layout>{page}</Layout>

export default CulturalTagPage
